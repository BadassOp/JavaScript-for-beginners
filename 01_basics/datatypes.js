"use strict"; // treat all JS code as newer version

// alert("hello") // we are using node js not browser so it cannot be executed and will give error

/*console.log(3
    +
    3); */          // not a good practice, readability should be high

console.log("Shantanu Jayant");  // this is a good practice

let name="Shantanu"
let age=19
let isLoggedIn=true

// datatypes (primitive dt)

/*number => range 2 to power 53
bigint(big integer) 
string => "" or ''
Boolean => true/false
null => standalone value
undefined => value not defined
Symbol (for uniqueness)*/


//object
console.log(typeof undefined);  // output is undefined
console.log(typeof null);   // output is object
